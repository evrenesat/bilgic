'use strict';

describe('bilgic.play module', function() {

  beforeEach(module('bilgic.play'));

  describe('play controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var playController = $controller('PlayController');
      expect(playController).toBeDefined();
    }));

  });
});