'use strict';

describe('bilgic.games module', function() {

  beforeEach(module('bilgic.games'));

  describe('games controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gamesController = $controller('GamesController');
      expect(gamesController).toBeDefined();
    }));

  });
});